<% provide(:title, @user.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
      <%= @user.name %>
      </h1>
      <h1>
        <% if @zines.any? %>
          <ul>
            <%= render partial: 'shared/zine', collection: @zines %>
          </ul>
        <% end %>
      </h1>
    </section>
  </aside>
</div>